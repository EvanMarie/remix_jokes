export default function TestChildRoute() {
  return (
    <div>
      <p>
        Text inside of index.tsx inside the test+ folder, which renders in the
        outlet of (test)_layout.tsx.
      </p>
    </div>
  );
}
